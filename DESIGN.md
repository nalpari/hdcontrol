# DESIGN.md

현대콘트롤전기 사이트의 시각 시스템. `docs/design/` 시안에서 실제로 만들어진 것을
기록한 것이지, 만들기 전에 쓴 규칙서가 아니다.

구현: `docs/design/assets/css/hd.css`

## 세계

이 회사의 제품은 전부 강판에 **명판**이 리벳으로 박혀 나간다. 명판은 제조사가 자기
이름과 정격을 책임지고 각인하는 자리다. 사이트 전체가 그 명판의 문법을 쓴다.

- 지면은 도장된 강판이다. 페이지 배경은 면이지 그라데이션 무대가 아니다.
- 그 위에 얇은 판이 올라간다. 판이지 카드가 아니다. 두께는 상단 1px 하이라이트와
  아래 그림자로 한 번만 선언한다. 중첩하지 않는다.
- 글자는 각인된다. 홈 그림자는 위쪽에 진다(`.engraved`). 큰 제목에만 쓴다.
- 표시등은 점이다. 면적을 차지하지 않는다.

거부한 것: 파란 그라데이션 히어로 + 아이콘·제목·설명 카드 12개의 제조사 템플릿,
그리고 그 예측 가능한 반대편인 블루프린트 모눈 위의 도면 감성.

## 색

전략은 **절제(Restrained)**다. 중성 두 계열 + 브랜드 레드 하나.
레드는 표시등, 1차 액션, 활성 상태에만 쓴다. 넓은 면을 칠하지 않는다.

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--ground` | `#101316` | 페이지 지면 |
| `--ground-deep` | `#0A0C0E` | 가라앉은 띠, 히어로 바닥 |
| `--plate` / `--plate-hi` | `#171B1F` / `#1E2329` | 명판 1 (아노다이즈드 그레이). 제품 |
| `--navy` / `--navy-hi` | `#142334` / `#1B3049` | 명판 2 (딥 네이비). 회사, 자격, 증빙 |
| `--engrave` | `#E9EDF0` | 각인 채움. 제목 |
| `--text` | `#B3BCC4` | 본문 |
| `--text-dim` | `#838D96` | 보조. 지면 대비 5.4:1 |
| `--signal` | `#ED1B23` | 로고 원색. 표시등, 테두리, 밑줄 |
| `--signal-face` | `#E01A21` | 흰 글자를 얹는 면. 대비 4.8:1 |
| `--signal-lit` | `#FF4A3D` | 포커스 링, 오류, 열린 상태 |
| `--arc` | `#F79636` | 로고 아크의 주황. 예비 |

`--signal`과 `--signal-face`가 나뉘어 있는 이유: 로고 원색 `#ED1B23` 위의 흰 글자는
4.4:1로 WCAG AA에 미달한다. 표시등과 테두리는 원색을 그대로 쓰고, 글자를 얹는 면만
한 단계 낮췄다.

두 명판 색은 의미를 나눈다. 제품은 회색 판, 회사와 자격은 네이비 판.

## 글자

- **Wanted Sans Variable** — 각인과 본문 전부. 네모진 산업용 그로테스크로 기계 각인
  글자에 가깝다. 800 weight를 제목에, 400/600을 본문에 쓴다.
- **JetBrains Mono Variable** — 치수, 모델코드, 특허번호, 연도, 전화번호. **측정값에만**
  쓴다. "기술적으로 보이려고" 쓰지 않는다.

자간은 제목 `-0.03em`, 소제목 `-0.025em`, 본문 기본. `-0.04em`을 넘지 않는다.
`word-break: keep-all`로 한국어를 어절 단위로 끊는다.

본문 measure 68ch, `.lede` 58ch.

## 형태

- 모서리 반경은 `4px` 하나. 작은 조작부(표식, 필터 칩, 스캔 속지)만 `2px`.
  알약 모양은 쓰지 않는다.
- 높이 선언은 판에 한 번. 테두리와 그림자를 겹쳐 쓰지 않는다.
- 리벳(`.riveted`)은 큰 판에만. 목록 행에는 붙이지 않는다.

## 구성 요소

| 이름 | 하는 일 |
|---|---|
| `.plate` / `.plate--navy` | 명판 |
| `.riveted` | 리벳 두 개. 큰 판 전용 |
| `.lamp` | 표시등. 렌즈로 그린다(radial-gradient). 바깥 헤일로 없음 |
| `.rack` / `.rack__row` | 제품 랙. 같은 크기 카드 12개를 대신한다 |
| `.lead` | 대표 제품 큰 판. 1.15fr / 1fr 비대칭 |
| `.data` | 명판의 정격 표기. 규격표, 회사개요, 주소 |
| `.log` | 연혁. 연도(mono) + 사실 + 번호 |
| `.certs` / `.cert` | 인증서 벽. 흰 종이는 밝은 면에 올린다 |
| `.scan` | 카탈로그 스캔과 도면. `#F4F5F6` 속지 |
| `.pcols` | 제품 사진(좌, sticky) + 설명·규격(우) |
| `.marks` / `.stamp` | 상태 표식. 색 알약이 아니다 |
| `.creed` | 사훈 네 줄 |
| `.subnav` / `.subnav--filter` | 페이지 탭(밑줄) / 필터 칩(눌린 표식). 둘은 달라야 한다 |

## 움직임

연출된 순간은 **하나**다. 첫 화면에서 각인이 흐림에서 자리를 잡고(`settle`) 표시등이
들어온다(`lamp-up`). 그 밖의 모든 움직임은 사용자 동작에 대한 응답이다. 호버, 열림,
포커스, 필터.

섹션마다 같은 페이드업을 붙이지 않는다. `prefers-reduced-motion: reduce`에서는
연출이 전부 사라진다. `transform`과 `opacity`만 애니메이트한다.

## 브라우저가 그리는 면

기본값으로 두지 않는다. 텍스트 선택(`::selection`), 스크롤바, 포커스 링,
밑줄 오프셋, 표의 숫자(tabular-nums) 전부 이 팔레트에서 칠했다.

## 접근성

- 본문·플레이스홀더 4.5:1 이상, 큰 글자 3:1 이상
- `:focus-visible`은 `--signal-lit` 2px, offset 3px
- 본문 건너뛰기 링크
- 메가메뉴는 `aria-expanded`, 아코디언은 `aria-expanded`, 필터는 `aria-pressed`
- 데스크톱 메가메뉴는 hover와 focus 양쪽으로 열린다
- 모바일 서랍은 Escape로 닫힌다

## 알려진 함정

- `.hdr`의 `backdrop-filter`가 fixed 자식의 컨테이닝 블록이 된다. 모바일 서랍은
  `bottom: 0`으로 높이가 잡히지 않으므로 `height`를 직접 준다.
- 닫힌 서랍이 오른쪽으로 밀려 가로 스크롤을 만든다. `html { overflow-x: clip }`으로
  막는다. `hidden`이 아니라 `clip`이어야 sticky 헤더가 산다.
- `.prose > *:first-child { margin-top: 0 }`이 래퍼 첫 제목의 위 여백을 지운다.
  여백은 래퍼에 준다.
- 제품 사진은 흰 배경이다. 전폭으로 깔면 어두운 지면에 흰 판이 박힌다.
  `.pcols`로 좌측 열에 넣거나 `.scan`으로 속지에 올린다.

## 이미지 출처

`docs/design/assets/img/`, `intro/img/`, `products/img/` — 현행 hdcontrol.co.kr에서
수집한 실제 제품 사진, 도면, 인증서. 178장.

`docs/design/assets/gen/` — Higgsfield 생성 이미지 5장 (2026-09-16). 실제 촬영물이
아니다. `docs/design/assets/gen/PROVENANCE.md` 참조. 실제 공장과 제품 사진으로
교체해야 한다.
